#include "main.h"

/**
 * _isuper - Check if a letter is supper
 * @x: The number to be checked
 * Return: 1 for upper letter or 0 for any else
 */

int _supper(int x)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
	return (0);
}#include "main.h"

/**
 * _isuper - Check if a letter is supper
 * @x: The number to be checked
 * Return: 1 for upper letter or 0 for any else
 */

int _supper(int x)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
	return (0);
}#include "main.h"

/**
 * _isuper - Check if a letter is supper
 * @x: The number to be checked
 * Return: 1 for upper letter or 0 for any else
 */

int _supper(int x)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
	return (0);
}
