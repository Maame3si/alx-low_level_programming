#include "main.h"

/**
 * isupper - Check if a letter is upper
 * @x: The number to be checked
 * REturn: 1 for upper letter or0 for any else
 */

int _isupper(int x)

{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
